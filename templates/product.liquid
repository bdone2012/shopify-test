{% layout "template.liquid" %}

{% block content %}
    <h1>{{ product.title | capitalize }}</h1>
    <img src="/images/glamour_view.png">

    {{ product.description }}
    <h3>{{ product.price | money }}</h3>
    <button>Add to cart</button>
{% endblock %}